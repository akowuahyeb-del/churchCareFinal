import { View, Text } from "react-native";

export default function ChurchSelectScreen() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Select Church Screen</Text>
    </View>
  );
}